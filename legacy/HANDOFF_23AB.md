# StateFlow — Session 23A/23B 交接

> **用途：** 給下一個 planning 對話（冷啟動的 Opus）看的。它的上下文是空的，只有 Project files 加上這份文件。
>
> **填寫時機：** A 與 B 都跑完之後。標 `<<< >>>` 的地方是你要填的。
>
> **原則：** 貼實體證據，不要轉述。「測試通過」是聲明，verbatim 輸出才是證據。

---

## 0. 這一輪做了什麼（一段話）

<<< 三到五句。例如：把行為矩陣純化成純行為規格；用一個看不到程式碼的 session 依矩陣盲寫驗收測試；同時用另一個 session 依同一份矩陣實作 config 驗證、per-step retry limit、schema 變更；然後把兩邊合流，分類失敗。 >>>

**當前 commit：** `<<< git rev-parse HEAD >>>`
**分支：** `<<< >>>`

---

## 1. 文件現狀（哪一份是權威）

| 檔案 | 地位 | 狀態 |
|---|---|---|
| `spec/BEHAVIOR_MATRIX.md` | **權威行為規格。**衝突時它贏 | v2.0，已純化 |
| `docs/StateFlow_Whitepaper_v1_0.md` | 架構與設計理由 | **多處落後於實作** |
| `docs/WHITEPAPER_V1_1_PATCHES.md` | 白皮書的修正清單 | <<< 消化了幾項？ >>> |
| `docs/BACKLOG.md` | 還沒成立的事 | 15 項白皮書修補 + 7 項未來 |
| `docs/OPERATIONAL_FACTS.md` | 怎麼啟動、連線、觀測 | 刻意不含行為契約 |
| `CLAUDE.md` | 常設紀律 | 已加 Authorship 一節 |
| `STATE_SNAPSHOT.md` | 專案歷史 | <<< 有沒有更新到本輪？ >>> |

> **`OPUS_REVIEW_PROTOCOL.md` 部分已過時：** 第 0 節的閱讀順序沒有矩陣；第 3 節是 Session 2 專用。流程的精神仍適用（快照是待查證的聲明，不是事實），細節不適用。

---

## 2. Session 23A（盲寫測試）產出

**執行方式：** 在 `~/Projects/sf-blind` 執行，該目錄只有四份規格文件，無任何 `.go`／`.sql`／既有測試。隔離靠實體不靠指令。

### 2.1 建立的檔案

```
<<< 貼檔案清單 >>>
```

### 2.2 覆蓋表

<<< 貼它產出的覆蓋表：矩陣每個 ID → 哪支測試的哪個函式，或標「種狀態層」「不變量健檢」「K 節不測」 >>>

**沒有歸屬的 ID：** <<< 列出，或寫「無」 >>>

### 2.3 它回報的規格問題（FINDINGS）

<<< 這一段可能比測試本身更有價值。規格的洞只有在有人試圖把它變成可執行斷言時才會現形 >>>

### 2.4 它最沒把握的測試

<<< 第 7 題的答案 >>>

---

## 3. Session 23B（實作）產出

**執行方式：** 在 `~/Projects/sf-impl`（git worktree，分支 `session-23b`）執行。對 `spec/` 唯讀。

### 3.1 動工前三件查核的實測結果

```
<<< 貼 \d workflows 的原始輸出等 >>>
```

### 3.2 完成的工作項

| # | 工作 | 對應矩陣 ID | 狀態 |
|---|---|---|---|
| 1 | Migration 000002 | N-20, N-21 | <<< >>> |
| 2 | Config 提交時驗證 | N-04 – N-13, N-17 – N-19 | <<< >>> |
| 3 | YAML/JSON 格式一致性 | N-01 – N-03 | <<< >>> |
| 4 | Per-step retry limit | N-24 – N-28 | <<< >>> |
| 5 | Planner 語意驗收 | D-02b, B-14, D-08, D-09 | <<< >>> |
| 6 | Replay 冪等與 GET /dlq | F-08, F-10 – F-13 | <<< >>> |
| 7 | Storage fail-fast | G-04, G-05 | <<< >>> |
| 8 | Retry delay 可見性 | H-04d | <<< >>> |
| 9 | 種狀態層 Go 測試 | C-04, C-05, C-07, C-08, E-08, E-09 | <<< >>> |
| 10 | 冷卻窗口查核 | E-08 – E-10 | <<< >>> |

### 3.3 凍結證據

```
$ git status --porcelain spec/
<<< 應為空 >>>

$ sha256sum spec/BEHAVIOR_MATRIX.md
<<< >>>
```

### 3.4 偏離點

<<< 逐條。**空白欄位可疑**——真實實作幾乎不可能零偏離 >>>

### 3.5 反向問答（第 8 題）

> **這一題可能是整輪最有價值的東西。** 經過 22 個 session，累積了不少沒被記錄的判斷。

<<< code 裡有哪些行為在矩陣裡找不到對應的列？逐條，附 file:function 與原始程式碼 >>>

**分類：** 每一條標明是 (a) 矩陣漏寫的規格、(b) 前人做的未記錄判斷、(c) 死碼。

---

## 4. 合流結果（把 A 的測試跑 B 的 code）

**這一節是整輪的重點產出。**

```
$ <<< 執行指令 >>>
<<< verbatim 輸出 >>>
```

### 4.1 失敗分類

| 分類 | 矩陣 ID | 說明 | 處置 |
|---|---|---|---|
| **測試錯** | <<< >>> | 斷言超出規格，或誤讀矩陣 | 改測試；若根因是矩陣寫得不清楚，一併改矩陣 |
| **Code 錯** | <<< >>> | 真的沒照規格做 | 改 code |
| **規格不清** | <<< >>> | 兩個獨立作者對同一句話做出不同但都合理的解讀 | **owner 裁定，然後補進矩陣與白皮書** |

> **第三堆是這整套流程真正的價值。** 兩個獨立作者對同一份規格產生分歧的地方，就是規格有洞的定義。

---

## 5. 仍未處理的事

<<< 從 BACKLOG 挑出本輪沒動、但下一輪該考慮的 >>>

已知的候選：

- **白皮書 §18 仍列著八項缺口，實際六項已關閉** —— 文件與現實落差最大的一處，任何讀白皮書的人都會低估專案完成度
- **`OPUS_REVIEW_PROTOCOL.md` 部分過時**
- **demo 尚未依新契約重寫** —— 本輪刻意排除，`retry_limit` 移到頂層之後 demo 建 workflow 的請求形狀會失效
- **usability**：ghcr.io 預先 build 的 image（現在別人要用得自己 build，這是採用門檻最大的一道牆）
- <<< 其他 >>>

---

## 6. 下一輪要決定什麼

<<< 你希望新 session 幫你做什麼？例如：排收尾與發布的順序／grill 你對系統的理解／規劃 k8s 部署／重寫 demo >>>

---

## 7. 給冷啟動 Opus 的提醒

- **你對現況一無所知，這是設計不是缺陷。** 任何「我記得這個專案是…」的念頭，回檔案查證。
- **矩陣是權威。** 白皮書多處落後，讀它時配 `WHITEPAPER_V1_1_PATCHES.md`。
- **BACKLOG 裡的東西都還沒實作**，不要當成規格。
- **這份交接是聲明，不是事實。** 需要查證時要求貼 verbatim 輸出與 git SHA。
- Owner 的溝通習慣：中文為主、技術名詞用英文；要直接的裁定與理由，不要選項清單；會直接指出你切得不夠乾淨——那是有效的訊號，收下。
